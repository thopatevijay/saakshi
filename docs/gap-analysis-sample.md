# Gap analysis — camera coverage of the Gujarat road network

> Generated by `npm run report:gap-analysis` at 2026-09-05T18:42:42.382Z from database `saakshi_d3_06`. Every figure below was computed on that run. **Do not hand-edit this file** — re-run the generator.

SAAKSHI · Gujarat Police Innovation Challenge 2026 · Pillar 1, Model 1 deliverable.

## 1 · Read this before any percentage

This report covers 50 of 80 registered cameras. The other 30 are **unassessable, not uncovered**: the upstream catalogue publishes `{id, name}` only, so they carry no coordinates and no district, and no spatial question can be asked of them at all. They are counted in `camera_coverage` — one row each, null geometry, with the reason recorded — precisely so that a later `count(*)` cannot be mistaken for the size of the estate.

A gap analysis is a recommendation to spend public money, so the limits are stated first rather than in a footnote:

- **80 cameras is a sandbox, not Gujarat.** The estate here is 30 measured sandbox cameras plus 50 geolocated sample rows. A statewide programme is of the order of tens of thousands. Nothing here is a finding *about Gujarat*; it is a demonstration that the method produces an auditable number when pointed at a real estate.
- **Every coverage cell is a circle.** `cameras` has no bearing or azimuth column, so a directional field-of-view wedge is not expressible for any camera in the estate — not as a fallback for a few, for all of them. A disc over-counts a camera looking along one carriageway and under-counts a wide junction view. This is the crudest form of the model.
- **The radii are assumptions.** Nobody surveyed them. They are stated in §3 and are overridable; the report prints whichever values the run used.
- **No live VAHAN / SARTHI / eGujCop / AFIS / NAFIS connectivity, and no face recognition.** Neither is used or implied anywhere in this analysis.

## 2 · The headline

**Every metre of the 20.35 km this estate appears to cover is contributed by a camera nobody has verified.** Trusted-only coverage is 0.00 km — a delta of 20.35 km, or 100% of apparent coverage. The cause is measured and specific: 80 of 80 cameras have never had a health check run against them, so every one resolves to `band: null`. That is an absence of evidence, not a bad result — but a conventional coverage map would have drawn all 20.35 km of it in green.

| | cameras | covered | share of network |
|---|---:|---:|---:|
| All cameras | 50 | 20.35 km | 0.0093% |
| Trusted cameras only | 0 | 0.00 km | 0.0000% |
| ANPR-viable cameras only | 17 | 2.77 km | 0.0013% |

The third row is the one that matters for ANPR specifically, which is the only mandatory analytic: 17 of 50 placed cameras are classified `anpr_viable`, and only those can be expected to return a readable plate. A `detection_only` camera sees the vehicle and reads nothing.

### The gap that a map cannot draw

Mapping where cameras *are* is a map anyone can draw. The more useful distinction is between *"no camera here"* and *"a camera here that sees almost nothing"* — and the estate has a measured example of the second:

> `cam03` returned **67** sightings in the same city and the same hour that `cam04` returned **33,548** — a 500x spread across eight cameras. `cam03` is not broken: it decoded 5,582 frames cleanly at 23.16 fps. It simply sees almost no vehicles. *(D1-09, #13)*

A coverage polygon drawn for `cam03` would be the same size and the same colour as `cam04`'s. Yield is the signal that separates them, and it is not a geometric property — which is why this report treats coverage area as necessary but not sufficient, and why the trust band, not the polygon, decides whether a metre of road counts as covered.

The same argument applies to time of day: **8 of 30 measured cameras fail `night_usable`** *(D1-05, #9)*. A junction covered only by a night-blind camera is uncovered for half of every day, and no single coverage percentage can express that.

## 3 · Method

### 3.1 · The coverage cell

| geometry class | radius | provenance |
|---|---:|---|
| `anpr_viable` | 60 m | assumed |
| `detection_only` | 120 m | assumed |
| `unclassified` | 60 m | assumed |

A plate needs roughly 60–80 px of width to be read. At the resolutions this estate actually runs — six distinct ones, twelve of thirty at 854x480 *(D1-05, #9)* — the plate-readable zone is tens of metres, not hundreds. `unclassified` takes the conservative figure: the absence of a classification is not evidence of a good one.

### 3.2 · The denominator

Covered share is expressed against **218026.20 km** over **5,40,584 ways**. `road_network` holds these classes and no others — `service`, `track`, `path`, `footway` and `cycleway` were excluded at import, because a coverage percentage computed over farm tracks and car-park aisles is not a number to put in front of a reviewer *(D3-01, #24)*:

| class | ways | km |
|---|---:|---:|
| `residential` | 4,20,160 | 73719.2 |
| `tertiary` | 36,708 | 51432.5 |
| `unclassified` | 46,348 | 50848.5 |
| `primary` | 12,222 | 18935.4 |
| `trunk` | 9,120 | 11763.0 |
| `secondary` | 7,261 | 9123.4 |
| `motorway` | 1,446 | 1263.7 |
| `tertiary_link` | 889 | 227.8 |
| `living_street` | 2,222 | 216.6 |
| `trunk_link` | 1,739 | 215.7 |
| `motorway_link` | 254 | 112.3 |
| `primary_link` | 1,709 | 72.3 |
| `road` | 135 | 55.9 |
| `secondary_link` | 371 | 39.9 |

### 3.3 · Reconciliation

It would be trivial to define `uncovered := total - covered` and report a perfect reconciliation that proves nothing. Instead `ST_Intersection` and `ST_Difference` are evaluated **independently** in EPSG:32643 (UTM 43N — metres, correct for Gujarat) over every way that comes within reach of a cell, and their sum is checked against those ways' own length. Ways outside that candidate set are 100% uncovered by construction, with no floating-point arithmetic involved.

| slice | candidate ways | candidate km | covered | uncovered | reconciliation error |
|---|---:|---:|---:|---:|---:|
| All cameras | 261 | 77.04 km | 20.35 km | 56.69 km | 0.000000 m |
| Trusted cameras only | 0 | 0.00 km | 0.00 km | 0.00 km | 0.000000 m |
| ANPR-viable cameras only | 43 | 15.61 km | 2.77 km | 12.84 km | 0.000000 m |

Tolerance is **1 m**. Every slice above is within it, and the generator fails loudly rather than publishing a report if one is not.

### 3.4 · Trust

A camera counts towards trusted coverage when the API resolves its band to `trusted` **and** it is not vetoed by the focus rule. The band is never re-derived from `trust_score >= 70`: an unreachable camera keeps its last good score, so arithmetic on the stored number paints a camera that went dark yesterday green *(D1-06, #10)*. The veto exists because an additive score cannot express a necessary condition — a camera that cannot produce a readable image produces nothing for ANPR whatever else is true of it, and `cam22` scores 55 and bands `degraded` while being effectively blind.

| band | cameras | of which placed |
|---|---:|---:|
| never probed | 80 | 50 |

## 4 · Junctions with zero trusted coverage

A junction here is **a point where at least 3 distinct ways of class `motorway`, `trunk`, `primary`, `secondary`, `tertiary` terminate**. That is a topological definition derived from way endpoints, not an OSM junction tag — the import keeps ways only. It therefore **misses** any junction expressed as one way passing through another without a shared endpoint, and it deliberately ignores residential and unclassified roads, because a junction of two residential lanes is not what a coverage plan is about.

**6,750 of 6,750** such junctions have no trusted camera covering them. None is covered.

The 25 highest-degree uncovered junctions, with coordinates:

| # | lon | lat | ways meeting | nearest named way | nearest trusted camera |
|---:|---:|---:|---:|---|---:|
| 1 | 72.570734 | 23.186064 | 6 | Amba Township Main Road | none |
| 2 | 73.787246 | 20.018091 | 6 | — | none |
| 3 | 68.977376 | 22.248284 | 5 | Station Road | none |
| 4 | 69.606003 | 21.640595 | 5 | MG Road | none |
| 5 | 70.459215 | 21.739962 | 5 | — | none |
| 6 | 70.617416 | 21.771482 | 5 | — | none |
| 7 | 70.829410 | 22.819765 | 5 | — | none |
| 8 | 70.848631 | 22.825083 | 5 | — | none |
| 9 | 71.352959 | 24.154854 | 5 | — | none |
| 10 | 71.457471 | 23.011033 | 5 | — | none |
| 11 | 71.493096 | 22.998067 | 5 | State Highway 7 | none |
| 12 | 71.628397 | 22.719469 | 5 | — | none |
| 13 | 72.064622 | 23.093275 | 5 | — | none |
| 14 | 72.137592 | 23.851330 | 5 | — | none |
| 15 | 72.337200 | 23.288769 | 5 | Kadi Underpass | none |
| 16 | 72.384048 | 23.567135 | 5 | Mehsana-Himatnagar Vijapur road | none |
| 17 | 72.390459 | 23.603880 | 5 | Station Road | none |
| 18 | 72.488464 | 23.241196 | 5 | — | none |
| 19 | 72.507990 | 23.050416 | 5 | Thaltej Road | none |
| 20 | 72.511614 | 23.027296 | 5 | Ramdevnagar road | none |
| 21 | 72.562200 | 23.009330 | 5 | Ashram Road | none |
| 22 | 72.570750 | 23.042429 | 5 | Gujrat vidhyapeeth road | none |
| 23 | 72.571280 | 23.022565 | 5 | Ashram Road | none |
| 24 | 72.575148 | 23.123451 | 5 | — | none |
| 25 | 72.596469 | 22.996112 | 5 | — | none |

Ranked by degree, then by longitude, so the ordering is deterministic and a re-run is diffable. A high-degree junction with no trusted camera is where the next camera goes.

## 5 · Where the coverage is

| district | covered km (all cameras) |
|---|---:|
| Junagadh | 0.81 km |
| Gandhinagar | 3.82 km |
| Rajkot | 1.71 km |
| Ahmedabad | 12.99 km |
| Gir Somnath | 1.03 km |

A per-department breakdown is **not** given, and the reason is a finding: `cameras.department_id` is NULL for every camera in this estate, so a departmental trust deficit has exactly one bucket — *no owning department recorded* *(D3-05, #28)*. Rendering that as a table would imply a structure the data does not have.

## 6 · Every camera, and what was assumed about it

All 80 rows in `camera_coverage`: 50 with a polygon, 30 with null geometry and a recorded reason. The polygons touch 261 distinct ways.

| camera | district | model | radius | assumption |
|---|---|---|---:|---|
| `GJ-AHM-0001` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0002` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0003` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0004` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0005` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0006` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0007` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0008` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0009` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0010` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0011` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0012` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0013` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0014` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0015` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0031` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0032` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0033` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0034` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0035` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0036` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0037` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0038` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0039` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0040` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0041` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0042` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0043` | Ahmedabad | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0044` | Ahmedabad | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-AHM-0045` | Ahmedabad | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0016` | Gandhinagar | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0017` | Gandhinagar | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0018` | Gandhinagar | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0019` | Gandhinagar | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0020` | Gandhinagar | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0046` | Gandhinagar | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0047` | Gandhinagar | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0048` | Gandhinagar | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0049` | Gandhinagar | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GAN-0050` | Gandhinagar | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-GIR-0026` | Gir Somnath | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-JUN-0021` | Junagadh | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-JUN-0022` | Junagadh | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-JUN-0023` | Junagadh | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-JUN-0024` | Junagadh | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-JUN-0025` | Junagadh | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-JUN-0027` | Junagadh | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-RAJ-0028` | Rajkot | disc | 60 m | Radius disc: classified ANPR-viable, so the disc is the plate-readable zone (60 m). No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-RAJ-0029` | Rajkot | disc | 120 m | Radius disc: classified detection-only, so the disc is the vehicle-visible zone (120 m) — a vehicle here is seen but its plate is not read. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `GJ-RAJ-0030` | Rajkot | disc | 60 m | Radius disc: geometry never classified, so the conservative plate-readable radius (60 m) is assumed rather than the larger detection radius. No bearing is available — `cameras` has no bearing column, so a directional wedge is not expressible for any camera in the estate. |
| `cam01` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam02` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam03` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam04` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam05` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam06` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam07` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam08` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam09` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam10` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam11` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam12` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam13` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam14` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam15` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam16` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam17` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam18` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam19` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam20` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam21` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam22` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam23` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam24` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam25` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam26` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam27` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam28` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam29` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |
| `cam30` | — | none | — | No coordinates. The upstream catalogue publishes {id, name} only, so this camera cannot be placed and contributes nothing to any coverage figure — it is unassessable, not uncovered. |

## 7 · Figures this run did not measure

These were measured against the live sandbox feed by earlier tickets. The feed is not held during report generation, so they are **cited, not recomputed**, and no number in §2–§5 depends on them:

| fact | value | source |
|---|---|---|
| Per-camera sighting yield, same city, same hour | a 500x spread — cam04 33,548 · cam08 24,462 · cam05 17,622 · cam01 13,725 · cam02 12,169 · cam06 7,092 · cam07 4,132 · cam03 67 | D1-09 (#13) |
| cam03 is not broken | decoded 5,582 frames cleanly at 23.16 fps — it simply sees almost no vehicles | D1-09 (#13) |
| Cameras failing night usability | 8 of 30; luma means range 8.40 to 135.19 | D1-05 (#9) |
| Distinct resolutions across the measured estate | six — 854x480 x12 · 1920x1080 x11 · 1280x960 x3 · 1280x720 x2 · 640x480 · 960x576 | D1-05 (#9) |
| Cameras effectively blind on focus (quality 0, a disqualifier regardless of band) | cam22 (blur 0.011) and cam09 (blur 2.047) | D1-05/D1-06 (#9, #10) |
| Cameras declaring a retention period | 0 of 30 | D3-05 (#28) |
| Road network | 540,584 GiST-indexed ways from Geofabrik western-zone, clipped to Gujarat | D3-01 (#24) |

## 8 · What would make this report better

- **A bearing per camera.** One column turns every disc into a wedge and roughly halves the over-count. It is the single highest-value schema change this analysis suggests.
- **Coordinates for the measured estate.** The thirty cameras with real trust measurements are the thirty that cannot be placed. Until the catalogue publishes a location — or an operator supplies one — the trusted-coverage figure is computed over sample rows.
- **A health check against the placed cameras.** Trusted coverage cannot be non-zero while every placed camera is `band: null`.
- **Measured radii.** A single afternoon with a test plate at three distances would replace the three assumed constants in §3.1 with measurements.

---

Regenerate with `npm run report:gap-analysis`. Engine: `packages/api/src/services/coverage.ts`. Renderers: `packages/api/src/services/gap-report.ts`.
