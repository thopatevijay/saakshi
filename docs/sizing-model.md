# Infrastructure sizing — Statewide (80,000 cameras)

> Generated by `npm run export:sizing`. Do not edit by hand: this file is the output of the same
> model the `/sizing` screen runs, and editing it here would let the document and the product
> disagree. Change the model or the scenario instead. Source: D3-08 (#31).

The estate size on the challenge's `/problems` page. 30% continuous ANPR is PROJECT.md section 9's own assumption — road-facing cameras are about a third of the estate — kept so the two can be compared line by line.

## 0 · Scenario

| Input | Value |
|---|---|
| Cameras | 80,000 |
| Continuous ANPR coverage | 30% |
| Analysed at the edge | 100% |
| Events per camera per day | 21,168 |
| Vehicle passages per event | 43.62 |
| Metadata retention | 365 days |
| Crop retention | 90 days |
| Accelerator | Apple Silicon MPS — extrapolated to a saturated node |

**The mean is not the median.** D1-09 measured a **500x** spread in sighting yield across eight
cameras in one city in one hour — `cam04` produced 33,548 and `cam03` produced 67. `cam03` is not
broken; it decoded 5,582 frames and watches a quiet road. Any single events-per-camera figure,
including the one in the table above, describes no actual camera. Size retained storage from the
mean and peak backhaul from the busy tail, and never present either as typical.

## 1 · Backhaul

| | Gbps |
|---|---|
| Every camera streamed centrally (Model 4 as written) | **160.00** |
| Video still travelling under this split | 0.000 |
| Events from the 80,000 cameras analysed at the edge | **0.056** |
| Total under this architecture | **0.056** |
| **Reduction** | **2,847.9x** |

At a full edge split the ratio is **2,847.9x**: video stays where it is and
only events travel, with video pulled on demand per incident over the existing feed path.

The arithmetic, so it can be checked without the source:

- Video: 80,000 cameras x 2.0 Mbps = 160.00 Gbps
- Events: 80,000 cameras x 21,168 events/day x 358.3 B x 8 / 86,400 s = 0.056 Gbps

## 2 · Compute

24,000 of 80,000 cameras run continuous ANPR (30%).

| Accelerator class | Streams each | Accelerators needed | Provenance |
|---|---|---|---|
| Apple Silicon MPS — demonstrated | 8.0 @ 4.00 fps | 3,000 | **measured** |
| Apple Silicon MPS — extrapolated to a saturated node | 100.0 @ 4.00 fps | 240 | assumed |
| NVIDIA T4 | 12.0 @ 25.00 fps | 2,000 | listed |
| NVIDIA L4 / A10 class | 25.0 @ 25.00 fps | 960 | listed |
| NVIDIA A100 | 60.0 @ 25.00 fps | 400 | listed |

**This scenario prices Apple Silicon MPS — extrapolated to a saturated node: 240 accelerators
across 33 district nodes — 7.3 per node
(8 racked).**

### Where the streams-per-accelerator figure comes from

It is measured, and it is measured twice, in two venues, with two different bottlenecks.

- **Demonstrated.** 8 concurrent camera streams with ANPR running, sustained over a 22-minute soak and a separate 5-minute ANPR run, zero reconnects, on one Apple Silicon laptop (D1-09 #13, D2-01 #15).
- **The node was 92% blocked in `decode()` the whole time** (D1-09 #13). On the government feed the bottleneck is the gateway, not the accelerator — which is the single most important sizing fact in this document.
- **Extrapolated linearly** to a saturated node: 100 streams at the sandbox's 4.00 effective fps, i.e. about 400 stream-fps of capacity.
- **Cross-check.** An independent run against local MediaMTX held 8 cameras at 25 fps with the loop 47.3% utilised, which extrapolates to about 423 stream-fps. Two venues, two bottlenecks, **5% apart**.
- Expressed at 25 fps per stream, our own extrapolation is about 16 streams per node — beside the 25 an L4/A10 is listed at. The first-pass estimate was fair.

**Honesty about the NVIDIA rows.** There is no NVIDIA GPU on this machine. The CUDA path in the
analytics worker is unit-tested by monkeypatching `torch`, never by execution, so every NVIDIA
figure above is vendor-listed and is labelled as such. Nothing in this document presents a CUDA
number as ours.

**The ANPR bottleneck is not the GPU.** Plate detection measured
252.0 ms p50 against eight concurrent cameras on the CPU,
against 18.91 ms for vehicle detection on the GPU — 13x — and the same plate
detector measured 46 ms against a *single* camera, so a capacity claim extrapolated from a
one-camera measurement is wrong by 5x (D2-01 #15). Buying GPUs will not move this line; raising
the size floor (`VEHICLE_MIN_BOX_PX`, `MAX_EXAMINE_PER_TRACK`) will.

## 3 · Storage

| | |
|---|---|
| Events written per day | 1,693,440,000 |
| Vehicle passages behind them per day | 73,871,028,000 |
| Metadata | **202.4 TB/year**, 202.4 TB retained at 365 days |
| Best-shot crops kept per day | 2,437,743,924 |
| Crops | **2,591.0 – 13,346.6 TB/year**, 638.9 – 3,291.0 TB retained at 90 days |
| **Total retained** | **841.3 – 3,493.3 TB** |

### Hot / warm / cold

| Tier | Window | Retained |
|---|---|---|
| Hot (NVMe) | 0–7 days | 53.6 – 259.8 TB |
| Warm (object store) | 7–90 days | 635.2 – 3,081.0 TB |
| Cold (archive) | beyond 90 days | 152.5 TB |

**Read the crop line with the assumption that drives it.** Crops follow vehicle passages, not
metadata rows, so summarising the rows does not reduce them — the same vehicles still drove past.
This scenario carries 923,388 passages per camera per day, which is D1-09's measured
8-camera rate extrapolated across a full 24 hours. **That extrapolation is the single largest
lever on every figure in this section**, and it overstates the overnight hours on every one of
those cameras. It is stated rather than silently corrected, because a correction factor would be
an unmeasured number wearing a measured number's clothes. Halve the event rate and halve this
table; the calculator exists so a reader can do exactly that and watch it move.

**Why crops are a range and metadata is not.** The metadata figure rests on a storage-layout
measurement that does not vary — 100,000 rows into a `like sightings including indexes` probe
table gave 195.1 B of heap and 327.4 B including indexes. The crop figure rests on a measurement
D2-02 explicitly marked provisional: mean 2,912 B, but taken on 682x384–1536x864 replay frames
with a median box long edge of 75 px, so a 1080p live feed will produce larger crops. The honest
statement is a band between 3 KB and 15 KB, and it is one command to close once the gateway is
free. Every storage and cost figure here is a range for that reason.

**The compression that is not provisional: 33 crops per 1,000 sightings.** Best-shot selection
discards about 97% of the crops a naive design would store, and that ratio is a property of the
tracker and the selector rather than of the frame size, so it holds at any resolution (D2-02 #16).

**Not claimed:** TimescaleDB columnar compression is not enabled on the sightings hypertable, so
the metadata figure above is uncompressed. That is headroom this model deliberately leaves on the
table rather than counting.

## 4 · Cost

Every unit cost below is an editable assumption, and the calculator shows it as one. A
single-figure cost estimate for an estate this size would be the least credible number on the
page, so each line carries the crop-size band and a further +/-25% uncertainty on top of it.

| Line | Per year | Basis |
|---|---|---|
| Backhaul capacity | INR 2.02 lakh – INR 3.37 lakh | 0.056 Gbps x 1,000 x INR 400/Mbps/month x 12 |
| Storage, all tiers | INR 54.54 lakh – INR 4.25 crore | hot 53.6–259.8 TB, warm 635.2–3081.0 TB, cold 152.5–152.5 TB at INR 2500 / 700 / 180 per TB-month |
| Power and cooling | INR 85.15 lakh – INR 1.42 crore | 240 accelerators x 450 W x 8,760 h x PUE 1.5 x INR 8/kWh |
| Operations staff | INR 81.00 lakh – INR 1.35 crore | ceil(240 / 40) FTE x INR 18,00,000/year |
| Software licences | INR 0 | 80,000 cameras x INR 0/camera/year — the whole stack is open source |
| **Annual opex** | **INR 2.23 crore – INR 7.05 crore** | sum of the above |

| | |
|---|---|
| Capex (accelerators) | INR 11.70 crore – INR 19.50 crore |
| Capex amortised over 5 years | INR 2.34 crore – INR 3.90 crore/year |
| **Total annual cost** | **INR 4.57 crore – INR 10.95 crore** |
| **Per camera per year** | **INR 571 – INR 1,369** |

**The licence line is zero, and that is a fact about the repository rather than a promise.** The
whole stack is open source. The one proprietary dependency is the optional natural-language query
LLM, and it sits behind a `QueryCompiler` interface with an `ollama` local provider and a `none`
provider — with either selected the system is fully functional and fully open. A commercial
VMS-plus-ANPR estate prices this line per camera per year, and at this camera count that single
term would dominate every other number in this table.

## 5 · Reconciliation with `PROJECT.md` section 9

Section 9's figures were a first pass, and this ticket's acceptance criterion allows either
reproducing them or correcting them. Both happened: the backhaul and GPU figures reproduce
*exactly* under section 9's own constants, and the storage figures do not reproduce at all,
because section 9 sized three different outputs from three mutually inconsistent event rates.

### What reproduces exactly

Running `computeSizing` on section 9's own inputs — 80,000 cameras, 2 Mbps, 2,000 B/s of events,
400 B per event, 30% ANPR coverage, 25 streams per GPU, 33 district nodes:

| Section 9 says | Model gives | |
|---|---|---|
| 160 Gbps central video | **160.00 Gbps** | reproduced |
| ~1.3 Gbps metadata | **1.28 Gbps** | reproduced |
| ~125x | **125.0x** | reproduced |
| ~960 GPUs | **960** | reproduced |
| ~29 GPUs per district node | **29.1** | reproduced |

### What does not, and why

Section 9 quotes three different per-camera event rates in adjacent bullets:

- **432,000 events/camera/day** — implied by "~2 KB/s events", which is what produces its 1.3 Gbps.
- **200 events/camera/day** — stated outright in its metadata-storage bullet, which is what produces its 2.3 TB/year.
- **~1,212 events/camera/day** — implied by its crop bullet's "~40/day" at D2-02's measured 33 crops per 1,000 sightings.

The first two differ by **2,160x**. They cannot both be right, and no single scenario can reproduce both.
Run the reproduction above — which uses the *backhaul* rate — and the metadata storage it implies is
**5,046 TB/year**, not 2.3 TB/year.

### What the measured rates give instead

| Anchor | Events/camera/day | Provenance | Source |
|---|---|---|---|
| Per-frame sightings, 8-camera mean | 923,398 | **measured** | D1-09 (#13) — 112,817 sightings / 8 cameras / 1319.5 s |
| Per-frame sightings, busiest camera (cam04) | 2,196,701 | **measured** | D1-09 (#13) — cam04, 33,548 sightings in 1319.5 s |
| Per-frame sightings, quietest camera (cam03) | 4,387 | **measured** | D1-09 (#13) — cam03, 67 sightings in 1319.5 s |
| One summary row per track | 21,168 | **measured** | D1-09 (#13) rate divided by D2-01 (#15) 43.62 sightings per track |
| PROJECT.md section 9 storage bullet | 200 | assumed | PROJECT.md section 9, "~200 events/day" |

The correction is not that section 9 was wildly wrong — its backhaul assumption sits inside the
measured range, between the per-track and per-frame rates. The correction is that it used a
different rate for each output. `PROJECT.md` section 9 has been rewritten to derive all three
from one rate, with this document as its source.

## 6 · Every constant, and where it came from

34 constants: **17 measured** on this stack,
3 vendor-listed, 14 assumed. There is no
unattributed number in this model — a test fails the build if any constant lacks a provenance tag
or a source.

| Key | Constant | Value | Provenance | Source |
|---|---|---|---|---|
| `measuredStreamsPerNode` | Streams demonstrated concurrently on one node | 8.00 streams | **measured** | D1-09 (#13) 22-minute soak and D2-01 (#15) 5-minute ANPR run |
| `upstreamBoundShare` | Analytics wall time blocked waiting on the gateway | 0.92 fraction | **measured** | D1-09 (#13) — 8-camera sandbox soak |
| `cameraEffectiveFps` | Effective fps delivered per sandbox camera | 4.00 fps | **measured** | D1-09 (#13) — 8-camera, 22-minute soak |
| `venueBUtilisation` | Loop utilisation at real-time rates (local MediaMTX) | 0.47 fraction | **measured** | D1-09 (#13) — 681 s of loop work in a 180 s window across 8 threads |
| `inferenceP50Ms` | Vehicle detection latency, p50 | 18.91 ms | **measured** | D1-09 (#13) — YOLO11n, Apple Silicon MPS, 8 cameras concurrent |
| `plateDetectP50Ms` | Plate detection latency, p50 | 252.00 ms | **measured** | D2-01 (#15) — YOLO-v9-s ONNX on CPU, 8 cameras concurrent |
| `ocrP50Ms` | Plate OCR latency, p50 | 19.20 ms | **measured** | D2-01 (#15) — fast_plate_ocr ONNX on CPU |
| `motionGateSkipRatio` | Frames skipped by the motion gate | 0.33 fraction | **measured** | D1-09 (#13) — live sandbox traffic |
| `sightingsPerTrack` | Per-frame sightings per tracked vehicle | 43.62 sightings/track | **measured** | D2-01 (#15) — 27,918 sightings over 640 tracks |
| `cameraYieldSpread` | Per-camera sighting yield spread | 500.72 x | **measured** | D1-09 (#13) — cam04 33,548 against cam03 67, same city, same hour |
| `cropBytesPer1000Sightings` | Crop bytes per 1,000 sightings, measured | 96,214 B/1000 sightings | **measured** | D2-02 (#16) — 96,214 B = 94.0 KiB per 1,000 sightings |
| `gatewayThrottleSlowS` | Worst observed gateway response for a 1.3 KB fetch | 63.00 s | **measured** | D1-03 (#7) — same `cameras.json`, 4.2 s against 63 s |
| `videoBitrateMbps` | Per-camera video bitrate if streamed centrally | 2.00 Mbps | assumed | PROJECT.md section 9 first-pass estimate |
| `eventWireBytes` | One sighting event on the wire (JSON) | 358.30 B/event | **measured** | D3-08 — `JSON.stringify` of the shipped `Sighting` schema, blended 967:33 |
| `sightingRowBytes` | One sighting at rest in PostgreSQL, including indexes | 327.40 B/row | **measured** | D3-08 — 100,000 rows into a `like sightings including indexes` probe table |
| `cropsPer1000Sightings` | Best-shot crops kept per 1,000 sightings | 33.00 crops/1000 | **measured** | D2-02 (#16) — 838 crops from 25,367 sightings |
| `cropBytesMeasured` | One stored best-shot crop, measured | 2,912 B/crop | **measured** | D2-02 (#16) — 838 crops, 2,440,661 B, JPEG quality 82 |
| `cropBytesCeiling` | One stored best-shot crop, conservative ceiling | 15,000 B/crop | assumed | PROJECT.md section 9 first-pass estimate, retained deliberately |
| `districtNodes` | District edge nodes | 33.00 nodes | listed | Gujarat has 33 districts |
| `hotTierDays` | Hot tier window | 7.00 days | assumed | D3-08 modelling choice |
| `warmTierDays` | Warm tier window (cumulative) | 90.00 days | assumed | D3-08 modelling choice |
| `acceleratorNodeCapexInr` | Capex per accelerator, including its share of the host | 650,000 INR/accelerator | assumed | D3-08 modelling choice — Indian list pricing for an L4-class 1U inference server |
| `acceleratorNodePowerW` | Sustained draw per accelerator node | 450.00 W | listed | Vendor TDP for an L4-class 1U inference server under sustained load |
| `datacentrePue` | Power usage effectiveness | 1.50 x | assumed | D3-08 modelling choice — typical for a district-level room, not a hyperscale hall |
| `powerTariffInrPerKwh` | Electricity tariff | 8.00 INR/kWh | listed | Gujarat HT industrial tariff band, order of magnitude |
| `hotStorageInrPerTbMonth` | Hot tier storage | 2,500 INR/TB/month | assumed | D3-08 modelling choice — on-premise NVMe, amortised |
| `warmStorageInrPerTbMonth` | Warm tier storage | 700.00 INR/TB/month | assumed | D3-08 modelling choice — on-premise object store (MinIO on spinning disk) |
| `coldStorageInrPerTbMonth` | Cold tier storage | 180.00 INR/TB/month | assumed | D3-08 modelling choice — archive tier, retrieval measured in hours |
| `backhaulInrPerMbpsMonth` | Managed backhaul capacity | 400.00 INR/Mbps/month | assumed | D3-08 modelling choice — leased capacity on GSWAN-class connectivity |
| `nodesPerFte` | Accelerator nodes one engineer can operate | 40.00 nodes/FTE | assumed | D3-08 modelling choice |
| `opsFteInrPerYear` | Fully loaded cost of one operations engineer | 1,800,000 INR/year | assumed | D3-08 modelling choice |
| `softwareLicenceInrPerCameraYear` | Per-camera software licence | 0.00 INR/camera/year | **measured** | D3-08 — the shipped dependency set, verified in `docs/model-licences.md` |
| `hardwareRefreshYears` | Hardware refresh cycle | 5.00 years | assumed | D3-08 modelling choice |
| `costUncertainty` | Cost uncertainty band | 0.25 fraction | assumed | D3-08 modelling choice |

### Notes that change how a number should be read

- **Streams demonstrated concurrently on one node** — Eight of eight, no reconnects, ANPR on, sustained. This is what was *demonstrated* on one Apple Silicon laptop — and the node was 92% idle while doing it, so 8 is a floor set by how many cameras were pointed at it, not a ceiling set by the hardware.
- **Analytics wall time blocked waiting on the gateway** — D3-10 (#33) recomputed 93.9% from a different pair of published totals; the two use different denominators and must not be mixed. This model quotes D1-09's own headline 92% throughout. Either way the finding is the same: the node running the government feed is idle roughly fifteen-sixteenths of the time, so on that feed the bottleneck is the gateway, not the accelerator.
- **Effective fps delivered per sandbox camera** — Median of the effective (wall-clock) column: min 1.92 / median 4.00 / max 4.98. The streams *carry* 15–30 fps by PTS; the gateway *delivers* about 4. Every capacity claim about the government feed must use this column, never the PTS one — the difference is roughly tenfold.
- **Loop utilisation at real-time rates (local MediaMTX)** — 681 / (8 x 180) = 47.29%. The sources publish with `-re`, i.e. at real time, so 199.78 aggregate fps is what the pipeline *sustained* while holding all 8 cameras at 25 fps — a floor, not a ceiling. Vehicle detection only: this run did not carry the ANPR plate detector.
- **Vehicle detection latency, p50** — p95 47.02 ms. Latency under 8-way concurrency, not a serial cost — quoting it without the concurrency is meaningless.
- **Plate detection latency, p50** — The same model measured 46 ms against ONE camera and 252 ms against eight: eight decode threads serialise on one ONNX session behind a lock. A capacity claim extrapolated from the single-camera figure is wrong by 5x. This is 13x the vehicle detector and it runs on the CPU, so the ANPR bottleneck on this stack is not the GPU.
- **Plate OCR latency, p50** — Runs three times per track against plate detection once per tracked vehicle per examined frame: 4,127 detection calls against 83 OCR calls in the same run, a 50x call ratio and a 654x time ratio. Optimising the OCR is optimising the wrong stage.
- **Frames skipped by the motion gate** — 59.9% on the local MediaMTX mix. Each frame is compared against the last *inferred* one with a 2 s-of-PTS keep-alive, so a camera that is idle most of the time costs proportionally less. This is the lever: most cameras in a 100,000-camera estate are idle most of the day.
- **Per-frame sightings per tracked vehicle** — The compression available for free: one summary row per track instead of one per frame is a 43.6x reduction in metadata, and it is the same argument best-shot selection already makes for crops.
- **Per-camera sighting yield spread** — The single most important caveat in this model. An events/camera/day mean is not a median and must never be presented as a typical camera: cam03 is not broken, it decoded 5,582 frames and simply watches a quiet road. Size storage from a distribution, and size peak backhaul from the busy tail.
- **Crop bytes per 1,000 sightings, measured** — The measured product of the two constants above (33.0 x 2,912 = 96,096, within rounding). Carried explicitly because it is the figure D2-02 published and the one this ticket's AC names.
- **Worst observed gateway response for a 1.3 KB fetch** — Not an input to any output here. Carried because it is the third independent corroboration of the throttle that makes the effective-fps column what it is, alongside D1-09's ~4 fps and D3-07's 6 s HLS segment delivered in 22–49 s (0.12x–0.28x real time).
- **One sighting event on the wire (JSON)** — A plain sighting serialises to 348 B and a best shot with a plate read and two crop URIs to 659 B; blended at D2-02's measured 33 best shots per 1,000 sightings gives 358.3 B. Serialiser-measured, so it excludes stream framing and any transport compression — Valkey Streams field names and gzip both move it, in opposite directions. PROJECT.md section 9 assumed 400 B, which this corroborates as a fair conservative round-up.
- **One sighting at rest in PostgreSQL, including indexes** — Heap 19,505,152 B (195.1 B/row) plus indexes 13,189,120 B, total 32,735,232 B over 100,000 rows. Measured on `saakshi_d3_08` and the probe table dropped afterwards. This is a storage-layout fact, not a rate, so it does not depend on machine load. It excludes TimescaleDB columnar compression, which is not enabled on this hypertable and would reduce it substantially — headroom this model deliberately does not claim.
- **Best-shot crops kept per 1,000 sightings** — The figure in D2-02 that is NOT provisional. Best-shot selection discards about 97% of the crops a naive design would store, and that ratio is a property of the tracker and the selector rather than of the frame size — it holds at any resolution.
- **One stored best-shot crop, measured** — PROVISIONAL, and D2-02 says so in `docs/evidence-store.md` section 7. The replay frames were 682x384 to 1536x864 with a median box long edge of 75 px, so a 1080p live feed will produce larger crops. Do not adopt this as the estate figure; the model uses it as the LOW end of a band.
- **Per-camera software licence** — Zero, and this is a fact about the repository rather than a promise. The entire stack is open source. The one proprietary dependency is the optional NL-query LLM, and it sits behind a `QueryCompiler` interface with an `ollama` local provider and a `none` provider — with either of those the system is fully functional and fully open. A commercial VMS-plus-ANPR estate typically prices this line per camera per year, and at 80,000 cameras that term alone dominates every other number on this page.

## 7 · What this model does not claim

- **No figure here was measured today.** Every throughput constant is cited from the ticket that
  gathered it. D1-09 (#13) recorded its numbers while another worker loaded the same laptop and
  its own handoff instructs a re-measurement on a quiet checkout before anything quotes them;
  **that re-run belongs to D4-04**, and until it lands these figures carry D1-09's caveat with them.
- **The sandbox figures size the government feed, not hardware.** The gateway delivers about 4
  effective fps against streams carrying 15–30 by PTS, and the analytics node sits 92% blocked in
  `decode()`. Three independent measurements agree: D1-03 timed the same 1.3 KB fetch at 4.2 s and
  63 s, and D3-07 measured a 6-second HLS segment arriving in 22–49 s, 0.12x–0.28x real time.
- **No NVIDIA figure is ours.** There is no NVIDIA GPU on this machine.
- **The crop size is provisional** and is presented as a 3–15 KB band, per D2-02.
- **Cost unit rates are assumptions, not quotations.** Nothing here has been procured.
- **0 of 30 cameras in the Gujarat catalogue declare a retention period** (D3-05 #28), so the
  hot/warm/cold windows above are our proposal and not a reflection of departmental practice.
